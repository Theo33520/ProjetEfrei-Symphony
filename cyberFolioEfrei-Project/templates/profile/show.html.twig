{% extends 'base.html.twig' %}

{% block title %}ProfileEntity{% endblock %}

{% block body %}
    <h1>ProfileEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile_entity.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ profile_entity.address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ profile_entity.phone }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ profile_entity.country }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ profile_entity.city }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ profile_entity.zipCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profile_index') }}">back to list</a>

    <a href="{{ path('app_profile_edit', {'id': profile_entity.id}) }}">edit</a>

    {{ include('profile/_delete_form.html.twig') }}
{% endblock %}
